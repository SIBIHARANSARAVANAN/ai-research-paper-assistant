# AI Research Paper Assistant – Frontend

React + Vite single-page app with a clean, professional UI.

## Setup

```bash
cd frontend
npm install
# Optionally set a custom API base URL if your backend runs elsewhere:
# echo "VITE_API_BASE_URL=http://localhost:8001" > .env.local
npm run dev
```

Then open the printed URL (usually http://localhost:5173) in your browser.